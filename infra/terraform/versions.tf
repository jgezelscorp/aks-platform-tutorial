terraform {
  required_version = ">= 1.5"
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}
  # subscription_id supplied via ARM_SUBSCRIPTION_ID env var or -var.
  subscription_id = var.subscription_id
}
